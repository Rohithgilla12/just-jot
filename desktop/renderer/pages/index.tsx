import React from "react";
import { Authentication } from "../components/Authentication";

function Home() {
  return (
    <React.Fragment>
      <Authentication />
    </React.Fragment>
  );
}

export default Home;
